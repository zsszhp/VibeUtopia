# VibeUtopia 项目上下文矩阵 (Single Source of Truth)

> 本文件是 VibeUtopia 项目的**统一静态记忆源**，所有 AI IDE（Antigravity、Trae、Comate、CodeBuddy、Claude Code）通过软链接读取此文件，避免每次新会话重新搜索项目信息。

---

## 1. 项目概述

**VibeUtopia** — 社交媒体内容风控仿真平台。通过 AI 仿真多平台用户反应，预判内容发布后的风险，提供改写建议和决策辅助。

- **版本**: v0.5.0
- **远端仓库**:
  - Gitee: `https://gitee.com/zzsszhp/VibeUtopia.git`
  - GitHub: `https://github.com/zsszhp/VibeUtopia`
- **项目进度**:
  - MVP ~ V2+（阶段1-6）: ✅ 全部完成
  - V3.1 扩展平台覆盖至28平台: ✅ 完成
  - V3.2 本地模型部署（Ollama/vLLM）: ✅ 架构完成
  - V3.3 多语言内容风控: 🔜 待开发

---

## 2. 技术栈

### 2.1 后端
- **语言**: Python 3.11+
- **框架**: FastAPI + Uvicorn
- **ORM**: SQLAlchemy 2.0（支持 MySQL/SQLite 自动降级）
- **数据验证**: Pydantic 2.0
- **模型路由**: LiteLLM（多厂商统一 API）
- **向量存储**: ChromaDB（嵌入式，Agent 记忆）
- **图数据库**: Neo4j 5.x（知识图谱）
- **关系数据库**: MySQL 8.0（Docker），SQLite（降级）
- **异步**: asyncio（替代 Redis 消息队列）

### 2.2 前端
- **框架**: Vue 3 + TypeScript
- **UI库**: Naive UI（暗色主题）
- **图表**: ECharts 6 + D3.js
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **样式**: Tailwind CSS 4
- **构建**: Vite 8

### 2.3 基础设施
- **容器**: Docker Compose（MySQL 8.0 + Neo4j 5）
- **音视频处理**: OpenCV + PySceneDetect + ffmpeg-python + Pillow
- **音频转写**: 阿里 Paraformer（HTTP API）
- **OCR**: GLM-OCR / Qwen3-VL-Plus API
- **信号采集**: APScheduler + Feedparser + httpx

---

## 3. 目录结构

> **重要变更**：项目已重组，`backend/` → `src/backend/`，`frontend/` → `src/frontend/`，`config/` → `src/config/`，`cases/` → `data/test/cases/`，`docker-compose.yml` → `scripts/`

```
VibeUtopia/
├── src/                        # 源代码（重组后）
│   ├── backend/                # 后端核心
│   │   ├── main.py             # FastAPI 入口，WebSocket，生命周期管理
│   │   ├── config.py           # Settings 配置类（从 .env 读取）
│   │   ├── database.py         # SQLAlchemy 引擎，MySQL/SQLite 自动降级
│   │   ├── models.py           # ORM 模型（25+ 表）
│   │   ├── routes.py           # V1 核心 API（预审/上传/人格/记忆）
│   │   ├── routes_v3.py        # V3+ 扩展 API（多模态/信号/图谱/仿真/决策）
│   │   ├── routes_blogger.py   # 博主知识引擎 API
│   │   ├── routes_story.py     # 人生故事生成 API
│   │   ├── routes_local_models.py # 本地模型管理 API (V3.2)
│   │   ├── routes_resume.py    # 断点续传 API
│   │   ├── migrations/         # 数据库迁移脚本
│   │   ├── prompts/            # Prompt 模板（28平台 persona + 风控 prompt）
│   │   │   ├── prompt_config.yaml # Prompt 版本管理配置
│   │   │   ├── risk_assessment_v2.txt # V2风险评估Prompt
│   │   │   └── changelog.md    # Prompt 变更记录
│   │   └── services/           # 业务逻辑层
│   │       ├── llm_client.py   # LLM 统一调用 + 模型路由 + 多Key轮换
│   │       ├── analyzer.py     # 核心风控分析引擎
│   │       ├── resumable_analyzer.py # 可恢复分析器（断点续传）
│   │       ├── checkpoint_manager.py # 检查点管理器
│   │       ├── enhanced_analyzer.py # V2 增强分析（信号+实体+动态权重）
│   │       ├── risk_assessor.py # 风险评估（11维）
│   │       ├── rewriter.py     # 文案改写
│   │       ├── report_generator.py # 报告生成
│   │       ├── report_optimizer.py # 报告质量优化
│   │       ├── confidence_calculator.py # 置信度计算
│   │       ├── consistency_checker.py # 一致性检查
│   │       ├── evidence_chain.py # 证据链
│   │       ├── entity_risk_chain.py # 实体风险链
│   │       ├── dynamic_weights.py # 动态权重
│   │       ├── signal_matcher.py # 信号关联匹配
│   │       ├── story_risk_associator.py # 人格-风险关联
│   │       ├── trait_risk_mapper.py # 特质-风险映射
│   │       ├── platform_weights.py # 平台权重
│   │       ├── platform_simulator.py # 平台仿真
│   │       ├── platform_immersion.py # 平台浸泡
│   │       ├── persona_generator.py # 人格生成器
│   │       ├── persona_simulator.py # 人格仿真
│   │       ├── agent_simulator.py # Agent 仿真
│   │       ├── hardware_detector.py # 硬件检测
│   │       ├── vram_manager.py  # VRAM 管理
│   │       ├── local_model_manager.py # 本地模型管理 (V3.2)
│   │       ├── chroma_model_warmup.py # ChromaDB 模型预热
│   │       ├── backtest.py      # 回测引擎
│   │       ├── backtest_runner.py # 回测运行器
│   │       ├── backtest_cases.py # 回测案例库
│   │       ├── counterfactual_sim.py # 反事实仿真
│   │       ├── decision_advisor.py # 决策辅助
│   │       ├── competitor_comparator.py # 竞品对比
│   │       ├── blogger_history.py # 博主历史分析
│   │       ├── blogger_video_indexer.py # 博主视频索引
│   │       ├── blogger_knowledge_service.py # 博主知识服务
│   │       ├── topic_recommender.py # 话题推荐
│   │       ├── trend_predictor.py # 趋势预测
│   │       ├── prompt_version_manager.py # Prompt 版本管理
│   │       ├── prompt_optimizer.py # Prompt 优化
│   │       ├── prompt_ab_test_runner.py # A/B 测试
│   │       ├── batch_analyzer.py # 批量分析
│   │       ├── video_extractor.py # 视频文案提取
│   │       ├── video_embedding_store.py # 视频嵌入存储
│   │       ├── multimodal_analyzer.py # 多模态分析
│   │       ├── keyframe_extractor.py # 关键帧提取
│   │       ├── frame_ocr.py     # 帧OCR
│   │       ├── frame_risk.py    # 帧风险评估
│   │       ├── audio_analyzer.py # 音频分析
│   │       ├── audio_transcriber.py # Paraformer 音频转写
│   │       ├── cross_modal_detector.py # 交叉模态检测
│   │       ├── hybrid_retriever.py # 混合检索
│   │       ├── text_splitter.py # 文本分割
│   │       ├── transcript_detector.py # 转写检测
│   │       ├── p1_archetypes.py # P1 原型定义
│   │       ├── p2_archetypes.py # P2 原型定义
│   │       ├── persona_archetypes.py # 人格原型
│   │       ├── prompt_manager_cli.py # Prompt CLI 工具
│   │       ├── fine_grained/    # 细粒度视频理解模块（V3.3新增）
│   │       │   ├── pipeline.py  # 细粒度分析流水线
│   │       │   ├── dense_frame_scanner.py # 密集帧扫描器
│   │       │   ├── region_amplifier.py # 区域放大器
│   │       │   ├── symbol_detector.py # 符号检测器
│   │       │   ├── temporal_anomaly.py # 时序异常检测
│   │       │   ├── map_auditor.py # 地图审计器
│   │       │   └── code_tracer.py # 代码追踪器
│   │       ├── signal/          # 信号采集模块（11个文件）
│   │       │   ├── fetcher.py   # 热榜聚合器（NewsNow API）
│   │       │   ├── rss_fetcher.py # RSS 采集器
│   │       │   ├── incremental.py # 增量检测器
│   │       │   ├── event_detector.py # 事件检测与聚类
│   │       │   ├── deep_crawler.py # 深度评论爬取
│   │       │   ├── sentiment.py # BERT-LoRA 情感标注
│   │       │   ├── keyword_extractor.py # LLM 关键词提取
│   │       │   ├── scheduler.py # 定时调度器
│   │       │   ├── models.py    # 信号数据模型
│   │       │   └── signal_config.yaml # 信号配置
│   │       ├── simulation/      # 仿真引擎模块
│   │       │   ├── engine.py    # 仿真调度器
│   │       │   ├── decision_engine.py # 仿真决策引擎
│   │       │   ├── rule_engine.py # 规则引擎
│   │       │   ├── time_model.py # 时间模型
│   │       │   ├── message_bus.py # 消息总线
│   │       │   ├── models.py    # 仿真数据模型
│   │       │   ├── recorder.py  # 仿真记录器
│   │       │   ├── scale_manager.py # 规模管理（4级）
│   │       │   ├── group_agent.py # 群体Agent（GA-S3）
│   │       │   ├── platforms/   # 平台仿真适配器（5个）
│   │       │   ├── propagation/ # 传播模型
│   │       │   ├── monitors/    # 仿真监控
│   │       │   └── replay/      # 仿真回放
│   │       ├── persona/         # 人格系统模块
│   │       │   ├── life_story_generator.py # 人生故事工厂（A/B/C三级）
│   │       │   ├── memory_stream.py # Memory Stream（ChromaDB优先）
│   │       │   ├── reflection_engine.py # 反思引擎
│   │       │   ├── social_network.py # 社交网络
│   │       │   └── quality_validator.py # 质量校验
│   │       ├── graph/           # 知识图谱模块
│   │       │   ├── graph_store.py # Neo4j 存储
│   │       │   ├── entity_extractor.py # 实体提取
│   │       │   └── ontology_generator.py # 本体生成
│   │       └── story_generation/ # 人生故事生成模块
│   │           ├── timeline_builder.py # 时间线构建
│   │           ├── scene_generator.py # 场景生成
│   │           ├── narrative_integrator.py # 叙事整合
│   │           └── personality_evolver.py # 人格演化
│   ├── config/                  # 静态配置
│   │   └── model_config.yaml    # 多模型路由配置
│   └── frontend/                # 前端工程
│       ├── src/
│       │   ├── api/index.ts     # API 调用封装
│       │   ├── components/      # UI 组件（18个）
│       │   ├── views/Workbench.vue # 主工作台
│       │   ├── stores/index.ts  # Pinia 状态
│       │   └── styles/main.css  # 全局样式
│       ├── package.json         # Node 依赖
│       ├── pnpm-lock.yaml       # pnpm 锁文件
│       └── vite.config.ts       # Vite 配置
├── data/                        # 运行时数据
│   ├── config/                  # 运行时配置（graph/signal/model_config.yaml）
│   ├── reports/                 # 分析报告输出
│   ├── test/cases/              # 回测案例库（从 cases/ 迁移）
│   │   ├── paperwork/           # 文本案例（36个）
│   │   ├── video_transcript/    # 视频转写案例
│   │   └── link/                # 链接案例
│   └── video_transcripts/       # 视频转写JSON
├── scripts/                     # 脚本工具
│   ├── docker-compose.yml       # Docker 配置（MySQL + Neo4j）
│   ├── setup.sh / setup.ps1     # 环境配置脚本
│   ├── start.sh / start.ps1     # 启动脚本
│   ├── setup_mysql.py           # MySQL 初始化
│   └── test_api_key.py          # API Key 测试
├── tests/                       # 测试
│   ├── video/                   # 4个音视频测试案例（Git LFS，不可删除）
│   ├── scripts/                 # 测试脚本
│   ├── comprehensive_test.py    # 综合测试
│   ├── vlm_*.py                 # VLM 视觉模型测试
│   └── optimized_test*.py       # 优化测试
├── docs/                        # 项目文档（00-30）
├── references/                  # 参考资源
│   ├── analysis/                # 26个技术分析文档（已上传git）
│   ├── projects/                # 25+开源项目源码（本地保留，不上传git）
│   └── papers/                  # 25+PDF论文（本地保留，不上传git）
├── requirements.txt             # Python 依赖
├── .env.example                 # 环境变量模板
├── CLAUDE.md                    # Claude Code 指令（项目规范）
├── .monkeycode/MEMORY.md        # 多人协作记忆文件
└── .ai_context.md               # 本文件 — 统一静态记忆源
```

---

## 4. 环境配置

### 4.1 Python 依赖（requirements.txt 核心项）
```
fastapi>=0.110.0          # Web 框架
uvicorn[standard]>=0.29.0 # ASGI 服务器
sqlalchemy>=2.0.0         # ORM
pydantic>=2.0.0           # 数据验证
litellm>=1.35.0           # 模型路由
chromadb>=0.5.0           # 向量存储
neo4j>=5.0.0              # 图数据库
pymysql>=1.1.0            # MySQL 驱动
opencv-python>=4.8.0      # 视频处理
scenedetect>=0.6.1        # 场景检测
ffmpeg-python>=0.2.0      # FFmpeg 封装
httpx>=0.27.0             # HTTP 客户端
websockets>=12.0          # WebSocket
apscheduler>=3.10.0       # 定时调度
feedparser>=6.0.0         # RSS 解析
```

### 4.2 环境变量（.env 核心项）
```
# LLM 厂商（多Key逗号分隔，自动轮换）
LONGCAT_API_KEY=          # 主力模型
DEEPSEEK_API_KEY=         # 备用
ALIYUN_API_KEY=           # 多模态+音频转写
SILICONFLOW_API_KEY=      # 聚合模型
SENSENOVA_API_KEY=        # DeepSeek V4 Flash

# 模型路由
DEFAULT_PROVIDER=longcat  # 当前激活厂商

# 数据库
DATABASE_URL=mysql+pymysql://vibe_user:vibe_password@localhost:3306/vibeutopia
# 降级: sqlite:///./data/vibeutopia.db

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=vibeutopia2024

# 本地模型 (V3.2)
OLLAMA_BASE_URL=http://localhost:11434/v1
VLLM_BASE_URL=http://localhost:8000/v1
```

### 4.3 Docker 服务
```bash
cd scripts && docker compose up -d       # 启动 MySQL + Neo4j
cd scripts && docker compose down        # 停止
```
- MySQL: `localhost:3306` (vibe_user/vibe_password)
- Neo4j Browser: `http://localhost:7474`
- Neo4j Bolt: `localhost:7687`

### 4.4 启动命令
```bash
# 后端（注意：代码在 src/backend/ 下，但 PYTHONPATH 设置后可直接引用 backend 包）
PYTHONPATH=src uvicorn backend.main:app --reload --port 8000
# 前端
cd src/frontend && pnpm install && pnpm dev
# 一键启动
bash scripts/start.sh
# 环境配置
bash scripts/setup.sh
```

---

## 5. API 端点速查

### 5.1 V1 核心 API（/api/v1）
| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /review | 提交内容预审（text/video/mixed） |
| GET | /review/{task_id} | 获取预审结果 |
| GET | /review/{task_id}/progress | 获取分析进度 |
| GET | /history | 历史记录（分页+筛选） |
| GET | /models | 可用模型和硬件等级 |
| POST | /upload | 上传视频文件（100MB上限） |
| POST | /persona/generate | 生成单个人格（A/B/C级） |
| POST | /persona/generate-batch | 批量生成人格 |
| POST | /memory/store | 存储记忆到 Memory Stream |
| POST | /memory/retrieve | 检索记忆（三因子：Recency+Importance+Relevance） |
| GET | /memory/status | Memory Stream 状态 |

### 5.2 V3 扩展 API（/api/v3）
| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /analyze-multimodal | 多模态分析（图片+文本） |
| POST | /upload-image-analyze | 上传图片分析 |
| POST | /transcribe-audio | 音频转写（Paraformer） |
| POST | /route-model | 获取最优模型路由 |
| GET | /available-models | 列出可用模型 |
| GET | /hardware-info | 硬件信息 |
| POST | /optimize-report | 优化报告质量 |
| GET | /signals/hotlist | 热榜信号 |
| GET | /signals/events | 检测事件 |
| POST | /signals/scheduler/start | 启动信号调度器 |
| GET | /graph/overview | 知识图谱概览 |
| GET | /graph/entity/{id} | 实体详情 |
| GET | /graph/paths | 实体间路径 |
| GET | /blogger/{id}/history | 博主历史分析 |
| POST | /competitor/compare | 竞品对比 |
| POST | /counterfactual/simulate | 反事实仿真 |
| POST | /decision/advise | 决策辅助 |
| POST | /simulation/scale | 设置仿真规模 |
| POST | /batch/submit | 批量分析 |
| GET | /polarization/warning | 极化预警 |
| POST | /backtest/consistency | 回测一致性检查 |

### 5.3 博主知识引擎 API（/api/v1/blogger）
| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /index | 创建视频索引 |
| GET | /{id}/status | 索引状态 |
| POST | /incremental | 增量索引 |
| POST | /ask | 知识问答 |
| POST | /search | 内容检索 |
| GET | /{id}/profile | 全知识画像 |
| POST | /contradictions | 观点矛盾检测 |
| POST | /timeline | 话题演变时间线 |

### 5.4 人生故事 API（/api/v1/story）
| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /generate | 生成人生故事 |
| GET | /{user_id} | 获取故事元数据 |
| GET | /{user_id}/timeline | 时间线详情 |
| GET | /{user_id}/scenes | 场景故事 |
| GET | /{user_id}/full | 完整故事 |
| POST | /{user_id}/evolve | 人格演化模拟 |

### 5.5 本地模型 API（/api/v3/local-models）
| 方法 | 路径 | 功能 |
|------|------|------|
| GET | /status | 完整状态 |
| GET | /ollama/status | Ollama 状态 |
| POST | /ollama/pull | 拉取模型 |
| GET | /vllm/status | vLLM 状态 |
| GET | /config-patch | 配置补丁 |
| GET | /setup-guide | 部署指南 |

### 5.6 WebSocket
| 路径 | 功能 |
|------|------|
| /ws/simulation/{sim_id} | 仿真状态实时推送 |
| /ws/review/{task_id} | 预审进度实时推送（5步骤） |

---

## 6. 数据库表速查

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| tasks | 分析任务 | id(UUID), status, mode, depth |
| risk_items | 风险项 | task_id, dimension, risk_score |
| platform_reactions | 平台反应 | task_id, platform, positive/neutral/negative |
| analysis_summaries | 分析摘要 | task_id, overall_score, dimensions_json |
| signal_records | 信号记录 | signal_id, source_platform, title, rank |
| seed_events | 种子事件 | event_id, signal_strength, status |
| agent_records | Agent 记录 | agent_id, platform, persona_json |
| social_relations | 社交关系 | agent_id_a/b, relation_type, weight |
| agent_memories | Agent 记忆 | agent_id, memory_type, content |
| simulation_records | 仿真记录 | sim_id, tick, agent_id, action_type |
| simulation_statuses | 仿真状态 | sim_id, status, total_agents |
| propagation_snapshots | 传播快照 | simulation_id, tick, polarization_index |
| propagation_edges | 传播边 | source/target_agent_id, influence_score |
| v2_analysis_results | V2增强结果 | task_id, mvp_score, v2_score |
| backtest_records | 回测记录 | case_id, mvp/v2_prediction |
| consistency_records | 一致性记录 | content_hash, overall_consistency |
| trend_predictions | 趋势预测 | prediction_id, pattern_name |
| video_analysis_records | 视频分析 | task_id, keyframe_count, overall_risk_score |
| blogger_profiles | 博主画像 | blogger_id, vocabulary/expression/topics_json |
| hotspot_correlations | 热点关联 | task_id, signal_id, correlation_score |
| entity_risk_chains | 实体风险链 | entity_name, total_risk_score |
| simulation_summaries | 仿真摘要 | task_id, polarization_index |

---

## 7. 模型路由配置

### 7.1 厂商列表
| 厂商 | 用途 | 模型级别 |
|------|------|---------|
| LongCat | 主力文本+视觉 | advanced(Thinking-2601, Omni-2603) / standard(Chat) |
| 阿里通义 | 多模态+音频 | advanced(qwen-vl-max, qwen-max) / standard(qwen-plus) / lite(qwen-turbo) |
| DeepSeek | 备用文本+视觉 | advanced(deepseek-vl2, deepseek-v4-flash) / standard(deepseek-chat) |
| 硅基流动 | 聚合模型 | advanced(Qwen2-VL-72B, DeepSeek-R1) / standard(DeepSeek-V3) |
| 商汤 SenseNova | DeepSeek V4 Flash | advanced(deepseek-v4-flash) |
| Ollama | 本地部署 | standard/advanced（按VRAM自动推荐） |
| vLLM | 本地高性能推理 | 运行时动态发现 |
| Paraformer | 音频转写专用 | standard(paraformer-v2) |

### 7.2 任务类型→模型级别映射
| 任务类型 | 级别 | 说明 |
|---------|------|------|
| life_story_generation | advanced | 人生故事生成 |
| persona_simulation | advanced | 人格仿真 |
| multimodal_analysis | advanced | 多模态分析 |
| risk_assessment | advanced | 风险评估 |
| default / text_understanding / platform_simulation | standard | 常规任务 |
| rewrite / json_extraction | lite | 轻量任务 |

### 7.3 Fallback 策略
同厂商同级别 → 同厂商低级别 → 跨厂商同级别 → 跨厂商低级别

### 7.4 重要注意事项
- LongCat Omni-2603 标记 `text=false`，**不用于纯文本任务**（会返回400错误）
- 多Key轮换：`LONGCAT_API_KEY=key1,key2`，配额耗尽自动切换
- 配置文件：`config/model_config.yaml`

---

## 8. 风控核心逻辑

### 8.1 风险评估11维度
政治敏感、法律合规、民族宗教、社会伦理、虚假信息、**事实错误**、**平台禁区**、**情绪极化**、**价值观倾向**、低俗擦边、侵权抄袭

### 8.2 风险等级（4档）
| 等级 | 分数范围 | 颜色 |
|------|---------|------|
| green | 0-25 | 安全 |
| yellow | 26-55 | 低风险 |
| orange | 56-75 | 中风险 |
| red | 76-100 | 高风险 |

### 8.3 红线规则
触碰即 HIGH 76+：政治敏感、法律合规、民族宗教、事实错误、平台禁区
多维度叠加：3个及以上维度触发时总分必须≥76

### 8.4 分析流程（5步骤）
understanding → assessment → signal → simulation → report

---

## 9. 平台覆盖（28个）

### 9.1 仿真适配器（5个深度适配）
bilibili, douyin, weibo, xiaohongshu, zhihu

### 9.2 Persona 模板（28个）
bilibili, douyin, weibo, xiaohongshu, zhihu, zhihu_zhuanlan, tieba, toutiao, kuaishou, wechat_channels, wechat_official, hupu, nga, v2ex, smzdm, taptap, jike, douban, maimai, instagram, tiktok_global, twitter, facebook, youtube, telegram, reddit, linkedin, youtube

---

## 10. 开发规范

### 10.1 强制 Git 工作流
1. 动手前 `git pull` 拉取最新
2. 有改动先 `git add -A && git commit -m "详细中文" && git push gitee && git push github`
3. 完成后必须 `git add -A && git commit -m "详细中文" && git push gitee && git push github`
4. commit message 必须用中文

### 10.2 编码规范
- 异步优先：所有 I/O 用 `async/await`，禁止 `time.sleep()`
- 类型标注：所有函数必须包含 Type Hints
- Pydantic 验证：输入输出使用 Pydantic 模型
- LLM 调用：必须通过 `llm_client.py`，不直接调用 API
- 数据库访问：通过 `database.py` 的 Session，不在路由中写复杂 SQL

### 10.3 不可删除的文件
- `tests/video/` — 4个音视频测试案例（Git LFS）
- `references/projects/` — 25+开源项目源码（本地保留）
- `references/papers/` — 25+PDF论文（本地保留）

---

## 11. 常用操作速查

### 11.1 回测
```bash
PYTHONPATH=. python3 tests/scripts/quick_backtest.py     # 快速回测
PYTHONPATH=. python3 tests/scripts/backtest_full.py       # 完整回测（25案例~5分钟）
```

### 11.2 信号采集
```bash
PYTHONPATH=. python3 tests/scripts/run_signal_collection.py  # 手动采集
PYTHONPATH=. python3 tests/scripts/run_t7_enhancement.py     # 信号增强
```

### 11.3 Prompt A/B 测试
```bash
PYTHONPATH=. python3 tests/scripts/run_prompt_ab_test.py
PYTHONPATH=. python3 -m backend.services.prompt_manager_cli  # CLI 工具
```

### 11.4 大规模模型测试
```bash
PYTHONPATH=. python3 tests/scripts/large_scale_model_test.py
```

---

## 12. AI 共享记忆策略

### 12.1 硬链接方案（Windows 推荐）
将各 IDE 的规则文件硬链接到本文件，实现一处修改、多端同步：

```powershell
# Windows 上使用 HardLink（不需要管理员权限，内容实时同步）
cd E:\z\project\my\VibeUtopia
New-Item -ItemType HardLink -Path ".antigravityrules" -Target ".ai_context.md" -Force
New-Item -ItemType HardLink -Path ".cursorrules" -Target ".ai_context.md" -Force
New-Item -ItemType HardLink -Path ".comaterules" -Target ".ai_context.md" -Force
New-Item -ItemType HardLink -Path ".codebuddyrules" -Target ".ai_context.md" -Force
```

> **Linux/macOS 用户**可使用 SymbolicLink：
> ```bash
> ln -sf .ai_context.md .antigravityrules
> ln -sf .ai_context.md .cursorrules
> ln -sf .ai_context.md .comaterules
> ln -sf .ai_context.md .codebuddyrules
> ```

> **注意**: `CLAUDE.md` 不建议软链接到此文件，因为 Claude Code 的 `CLAUDE.md` 需要包含 Skill 调用规则等 Claude 专属指令，与通用上下文不同。`CLAUDE.md` 应独立维护，但可引用本文件。

### 12.2 记忆文件矩阵
| 文件 | 用途 | 维护者 |
|------|------|--------|
| `.ai_context.md` | 统一静态记忆（本文件） | 所有 IDE 共享 |
| `CLAUDE.md` | Claude Code 专属指令 | Claude Code |
| `.monkeycode/MEMORY.md` | 多人协作动态记忆 | 所有开发者 |
| `.claude/projects/.../memory/` | Claude 持久化记忆 | Claude Code |

### 12.3 更新策略
- **静态信息**（目录结构、环境配置、API端点）→ 更新本文件
- **动态进度**（阶段完成、测试结果）→ 更新 `.monkeycode/MEMORY.md`
- **Claude 专属规则**（Skill 调用、工作流）→ 更新 `CLAUDE.md`
- 代码变更后，同步检查本文件是否需要更新

---

## 13. 技术债务

| 项目 | 当前状态 | 建议 |
|------|---------|------|
| routes.py 旧端点 | 76个路由(含5核心+71旧) | 逐步标记deprecated→删除 |
| echarts chunk | 1.1MB | code-split或换轻量图表库 |
| RiskItem 模型 | severity 是字符串 | 加 risk_score 数值列 |
| run_analysis 数据流 | analyzer 输出与前端不完全对齐 | 重构输出格式 |
| 视频文件上传 | 前端传文件名字符串 | 添加后端上传端点+前端FormData |
