# VibeUtopia 环境与待办事项

## 当前技术状态 (已确定最终方案)

| 组件 | 选型 | 状态 |
|------|------|------|
| **操作系统** | Ubuntu 22.04/24.04 LTS | **最终决定**。Docker 原生、CUDA 首推平台、生产一致性。 |
| **关系型 DB** | MySQL 8.0 | **最终决定**。Docker 镜像已就绪。 |
| **向量 DB** | ChromaDB | **最终决定**。嵌入式，用于 Agent 记忆。ext4 下性能最优。 |
| **图 DB** | Neo4j 5.x | **最终决定**。用于 GraphRAG 与关系分析。 |
| **消息/队列** | asyncio.Queue | **最终决定**。替代 Redis。 |
| **模型路由** | LiteLLM | **最终决定**。统一 API 接入。 |
| **Python** | 3.11+ | **必须**。3.9 已过 EOL。 |

## 文档指引与规范

- **[00_文档索引与阅读指引](./00_文档索引与阅读指引.md)**: 快速了解项目全貌。
- **[16_开发环境搭建指南](./16_开发环境搭建指南.md)**: Ubuntu 环境 Step-by-step 搭建手册。
- **[17_代码目录规范](./17_代码目录规范.md)**: 项目结构与编码准则。

## 蓝图对齐重构完成情况 (阶段1)

### 已完成

| 序号 | 任务 | 说明 |
|------|------|------|
| 1 | **17份**设计文档提交 | 按新蓝图重新规划的文档已入库（含索引、环境、规范） |
| 2 | 后端依赖清理 | 移除 Redis/Milvus 依赖，引入 ChromaDB/LiteLLM |
| 3 | Docker 配置更新 | `docker-compose.yml` 已包含 MySQL 和 Neo4j |
| 4 | 配置类更新 | `backend/config.py` 已同步最新选型默认值 |
| 5 | 后端API收敛 | 5个核心端点定义完成 |
| 6 | 前端暗色工作台 | Vue 3 + Naive UI 基础架构完成 |

## 需要你手动操作的事项

### 必须手动完成 (按 16 文档顺序)

1. **GitHub 推送**: `git push github main`
2. **Ubuntu 环境搭建**: 按照 **[16_开发环境搭建指南](./16_开发环境搭建指南.md)** 在 Ubuntu 上完成所有安装。
3. **Docker 启动**: `docker compose up -d` 启动 MySQL + Neo4j。
4. **API Key 配置**: 在 `.env` 中填入你的密钥。
5. **验证连接**: 运行 `uvicorn backend.main:app --reload` 确认 8000 端口可用。

---

## 下阶段重点 (阶段2: 平台人格与信号对齐)

- [ ] 实现 **[07_世界构建](./07_世界构建层设计.md)** 中的 Agent 模板工厂。
- [ ] 对接 **[06_信号采集](./06_信号采集层设计.md)** 的 API，获取实时热点。
- [ ] 在前端工作台展示 **[09_分析决策](./09_分析决策层设计.md)** 的"证据链"视图。

---

## 技术债务（非阻塞，后续优化）

| 项目 | 当前状态 | 优化建议 |
|------|---------|---------|
| 视频文件上传 | 前端传文件名字符串 | 需添加后端文件上传端点 + 前端FormData |
| echarts chunk 1.1MB | 构建成功但包较大 | 可code-split或换轻量图表库 |
| routes.py旧端点 | 76个路由(含5核心+71旧) | 后续阶段逐步标记deprecated→删除 |
| RiskItem模型 | severity是字符串非数值 | 后续可加risk_score数值列 |
| run_analysis数据流 | analyzer写入字段与前端期望不完全对齐 | 后续重构analyzer输出格式 |

## 已完成的清理

| 清理项 | 原依赖 | 替代方案 | 完成状态 |
|--------|--------|---------|---------|
| Redis | 缓存+消息总线 | asyncio.Queue + 内存LRU | 已移除 |
| Milvus | 向量检索 | ChromaDB内嵌式 | 已移除 |
| PaddlePaddle | 本地OCR | GLM-OCR / Qwen3-VL-Plus API | 已移除 |
| yt-dlp | 在线视频下载 | 仅本地视频文件 | 已移除 |
| Streamlit | 前端 | Vue3 + Naive UI | 已移除 |
| Element Plus | UI框架 | Naive UI (暗色主题) | 已替换 |
| 7路由 | 多页面导航 | 单页面/workbench | 已收敛 |
| 38+ API端点 | 分散功能 | 5核心端点(旧保留) | 已收敛 |
| 7302行routes.py | 大量重复路由 | 精简至2424行 | 已精简 |

## 后续阶段待办（阶段2-6）

- 阶段2: 25+平台覆盖，需设计平台人格模板
- 阶段3: 人生故事驱动人格系统，需ChromaDB+大模型API
- 阶段4: Memory Stream + 平台浸泡，需ChromaDB+Neo4j
- 阶段5: 万级Agent仿真，需GA-S3 Group Agent机制
- 阶段6: 博主服务（后置，主线稳定后考虑）
