# VibeUtopia 环境与待办事项

## 当前环境状态

| 项目 | 状态 | 版本/说明 |
|------|------|-----------|
| 操作系统 | Windows 11 | - |
| Python | 已安装 | 3.9.7 |
| Node.js | 已安装 | v24.14.1 |
| FFmpeg | 已安装 | 8.1.1 |
| PyTorch | 未安装 | GPU检测和本地模型fallback需要 |
| ChromaDB | 未安装 | 向量检索需要（阶段3-4） |
| PaddleOCR | 未安装 | 已设计移除，改用GLM-OCR/API |
| Neo4j | 未安装 | 知识图谱需要（Lite模式可跳过） |
| MySQL | 未安装 | SQLite降级可用（多用户时需安装） |

## 待安装/待处理事项

### 高优先级（影响核心功能）

| 序号 | 事项 | 说明 | 状态 |
|------|------|------|------|
| 1 | Python 3.9→3.10+ | 部分新库要求3.10+，ChromaDB要求3.8+（3.9可用但建议升级） | 待定 |
| 2 | PyTorch (CUDA) | 本地模型fallback需要，VRAM管理需要torch.cuda；无GPU则走全API模式(Lite) | 待安装 |
| 3 | ChromaDB | `pip install chromadb`，向量检索基础设施（阶段3-4需要） | 待安装 |

### 中优先级（增强功能需要）

| 序号 | 事项 | 说明 | 状态 |
|------|------|------|------|
| 4 | Neo4j | 知识图谱+实体风险链需要；Lite模式可跳过，MySQL中独立运行 | 待安装 |
| 5 | MySQL | 多用户/高并发场景需要；当前SQLite降级可用 | 待安装 |
| 6 | faster-whisper | 本地音频转写需要GPU+PyTorch；API降级可用(Paraformer) | 依赖PyTorch |
| 7 | GLM-OCR本地模型 | 0.9B参数OCR模型，需要GPU；API降级可用 | 依赖PyTorch |

### 低优先级（后置功能）

| 序号 | 事项 | 说明 | 状态 |
|------|------|------|------|
| 8 | Qwen3-VL-8B本地模型 | Pro模式画面理解fallback；API为主 | 依赖24GB GPU |
| 9 | Qwen3-8B/Qwen3-32B本地 | 风险评估/仿真本地fallback | 依赖大GPU |
| 10 | Playwright | 深度评论爬取降级方案；API优先可跳过 | 待安装 |

### 环境限制与决策

| 限制 | 影响 | 处理方式 |
|------|------|---------|
| Windows环境 | 部分Linux工具不可用 | 跳过，记录待办 |
| 无GPU/小GPU | 本地模型无法运行 | 走Lite模式（全API） |
| Python 3.9 | 部分新库兼容性 | 当前可用，后续考虑升级 |

## 已完成的清理

| 清理项 | 原依赖 | 替代方案 | 完成状态 |
|--------|--------|---------|---------|
| Redis | 缓存+消息总线 | asyncio.Queue + 内存LRU | 设计已移除 |
| Milvus | 向量检索 | ChromaDB内嵌式 | 设计已移除 |
| PaddlePaddle | 本地OCR | GLM-OCR / PaddleOCR-VL-1.5(独立) | 设计已移除 |
| yt-dlp | 在线视频下载 | 仅本地视频 | 待代码移除 |
| Streamlit | 前端 | Vue3 + Naive UI | 待代码移除 |

## 后续阶段待办（阶段2-6）

- 阶段2: 25+平台覆盖，需设计平台人格模板
- 阶段3: 人生故事驱动人格系统，需ChromaDB+大模型API
- 阶段4: Memory Stream + 平台浸泡，需ChromaDB+Neo4j
- 阶段5: 万级Agent仿真，需GA-S3 Group Agent机制
- 阶段6: 博主服务（后置，主线稳定后考虑）
