# VibeUtopia 环境与待办事项

## 当前环境状态

| 项目 | 状态 | 版本/说明 |
|------|------|-----------|
| 操作系统 | Windows 11 | - |
| Python | 已安装 | 3.9.7 |
| Node.js | 已安装 | v24.14.1 |
| FFmpeg | 已安装 | 8.1.1 |
| PyTorch | 未安装 | GPU检测和本地模型fallback需要 |
| ChromaDB | 未安装 | 向量检索需要（阶段3-4） |
| PaddleOCR | 已移除 | 改用API OCR (Qwen3-VL-Plus/GLM-OCR) |
| Neo4j | 未安装 | 知识图谱需要（Lite模式可跳过） |
| MySQL | 未安装 | SQLite降级可用（多用户时需安装） |

## 蓝图对齐重构完成情况 (阶段1)

### 已完成

| 序号 | 任务 | 说明 |
|------|------|------|
| 1 | 14份设计文档提交 | 按新蓝图重新规划的文档已入库 |
| 2 | 环境与待办文档 | 本文档 |
| 3 | 后端依赖清理 | 移除yt-dlp/PaddlePaddle/Redis/Streamlit依赖 |
| 4 | 后端API收敛 | 新增5个核心端点(POST /api/review, GET review/{id}, progress, history, models) |
| 5 | WebSocket进度推送 | 新增ws/review/{task_id}端点，对齐5步骤格式 |
| 6 | 前端依赖切换 | Element Plus → Naive UI，按需导入 |
| 7 | 前端路由重构 | 7路由 → 1路由(/workbench) |
| 8 | 前端API层重写 | 对接5核心端点，TypeScript接口定义 |
| 9 | 暗色主题三面板布局 | App.vue(左240+主+右320可折叠) + 底部进度条 |
| 10 | 左栏组件 | 上传区(3模式)+历史记录+25平台热力图 |
| 11 | 主内容区组件 | 分析流水线+风险仪表盘+7维雷达+平台反应 |
| 12 | 右栏组件 | 风险详情(证据链+置信度)+改写建议卡片 |
| 13 | WebSocket实时进度 | step_update/risk_alert/review_complete + 断连降级轮询 |
| 14 | Streamlit前端废弃 | 移至_archived/，start.bat已更新 |

### 已知限制

| 限制 | 影响 | 处理方式 |
|------|------|---------|
| GitHub推送偶发超时 | 双平台备份可能只有Gitee成功 | 定期手动重试 `git push github main` |
| routes.py 7072+行 | 旧端点保留未删，文件较大 | 后续阶段逐步标记deprecated→删除 |
| 视频上传为本地路径 | 未实现浏览器文件上传到服务器 | 需后端添加文件上传端点 + 前端FormData |
| 前端未验证构建 | dev server未实际运行验证 | 需`npm run dev`验证组件渲染 |

## 待安装/待处理事项

### 高优先级（影响核心功能）

| 序号 | 事项 | 说明 | 状态 |
|------|------|------|------|
| 1 | Python 3.9→3.10+ | 部分新库要求3.10+，ChromaDB要求3.8+（3.9可用但建议升级） | 待定 |
| 2 | PyTorch (CUDA) | 本地模型fallback需要，VRAM管理需要torch.cuda；无GPU则走全API模式(Lite) | 待安装 |
| 3 | ChromaDB | `pip install chromadb`，向量检索基础设施（阶段3-4需要） | 待安装 |
| 4 | 视频文件上传接口 | 前端需FormData上传→后端保存→返回路径供分析 | 待开发 |
| 5 | 前端构建验证 | `npm run dev` + `npm run build` 确认无报错 | 待验证 |

### 中优先级（增强功能需要）

| 序号 | 事项 | 说明 | 状态 |
|------|------|------|------|
| 6 | Neo4j | 知识图谱+实体风险链需要；Lite模式可跳过 | 待安装 |
| 7 | MySQL | 多用户/高并发场景需要；当前SQLite降级可用 | 待安装 |
| 8 | faster-whisper | 本地音频转写需要GPU+PyTorch；API降级可用(Paraformer) | 依赖PyTorch |
| 9 | GLM-OCR本地模型 | 0.9B参数OCR模型，需要GPU；API降级可用 | 依赖PyTorch |

### 低优先级（后置功能）

| 序号 | 事项 | 说明 | 状态 |
|------|------|------|------|
| 10 | Qwen3-VL-8B本地模型 | Pro模式画面理解fallback；API为主 | 依赖24GB GPU |
| 11 | Qwen3-8B/Qwen3-32B本地 | 风险评估/仿真本地fallback | 依赖大GPU |
| 12 | Playwright | 深度评论爬取降级方案；API优先可跳过 | 待安装 |

### 环境限制与决策

| 限制 | 影响 | 处理方式 |
|------|------|---------|
| Windows环境 | 部分Linux工具不可用 | 跳过，记录待办 |
| 无GPU/小GPU | 本地模型无法运行 | 走Lite模式（全API） |
| Python 3.9 | 部分新库兼容性 | 当前可用，后续考虑升级 |

## 已完成的清理

| 清理项 | 原依赖 | 替代方案 | 完成状态 |
|--------|--------|---------|---------|
| Redis | 缓存+消息总线 | asyncio.Queue + 内存LRU | 已移除 |
| Milvus | 向量检索 | ChromaDB内嵌式 | 已移除 |
| PaddlePaddle | 本地OCR | GLM-OCR / Qwen3-VL-Plus API | 已移除 |
| yt-dlp | 在线视频下载 | 仅本地视频文件 | 已移除 |
| Streamlit | 前端 | Vue3 + Naive UI | 已移除 |
| Element Plus | UI框架 | Naive UI (暗色主题) | 已替换 |
| 7路由 | 多页面导航 | 单页面/workbench | 已收敛 |
| 38+ API端点 | 分散功能 | 5核心端点(旧保留) | 已收敛 |

## 后续阶段待办（阶段2-6）

- 阶段2: 25+平台覆盖，需设计平台人格模板
- 阶段3: 人生故事驱动人格系统，需ChromaDB+大模型API
- 阶段4: Memory Stream + 平台浸泡，需ChromaDB+Neo4j
- 阶段5: 万级Agent仿真，需GA-S3 Group Agent机制
- 阶段6: 博主服务（后置，主线稳定后考虑）
